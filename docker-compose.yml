services:
  mbt:
    build: .
    ports:
      - "8086:8080"
    restart: unless-stopped

  # Development mode - uncomment below and comment above to use
  # mbt-dev:
  #   image: node:20-alpine
  #   working_dir: /app
  #   volumes:
  #     - .:/app
  #     - node_modules:/app/node_modules
  #   ports:
  #     - "5173:5173"
  #   command: sh -c "npm install && npm run dev -- --host"
  #   restart: unless-stopped

# volumes:
#   node_modules:
